var x,y;

function addition(x, y) {
  return (x + y);
}

function subtraction(x, y) {
  return (x-y);
}

function multiplication(x, y) {
  return x * y;
}

function division(x, y) {
  return x/y;
}

console.log('Total: ' + addition(5,6));
console.log('Subtraction: ' + subtraction(5,6));
console.log('Multiplication: ' + multiplication (5,6));
console.log('Division: ' + division(5,6));
